#!/usr/bin/env bash
# Generated by ai-automations — shared constants for the Notion project agent.
# Override any value via the environment. NOTION_TOKEN comes from CI secrets.

export NOTION_DATABASE_ID="${NOTION_DATABASE_ID:-{{NOTION_DATABASE_ID}}}"
# The status property's NAME (works for both `status` and `select` property types).
export STATUS_PROP="${STATUS_PROP:-{{STATUS_PROP}}}"
# Status option NAMES (matching is case-insensitive).
export STATUS_ANALYZE="${STATUS_ANALYZE:-{{STATUS_ANALYZE}}}"
export STATUS_IMPLEMENT="${STATUS_IMPLEMENT:-{{STATUS_IMPLEMENT}}}"
# Enabled handlers (comma-separated). Fully implemented: pages.
# databases (schema events) are routed by the relay but skipped by the
# resolver until their playbook lands — enabling them is harmless, just inert.
export ENABLED_HANDLERS="${ENABLED_HANDLERS:-{{HANDLERS}}}"
export AGENT_MARKER="${AGENT_MARKER:-{{AGENT_MARKER}}}"
export AI_BRAIN="${AI_BRAIN:-{{BRAIN_NAME}}}"
export AI_MODEL="${AI_MODEL:-{{AI_MODEL}}}"
export STATE_BRANCH="${STATE_BRANCH:-notion-agent-state}"
export STATE_DIR="${STATE_DIR:-.agent-state}"
export OUT_DIR="${OUT_DIR:-.agent-out}"
