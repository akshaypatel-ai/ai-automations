#!/usr/bin/env bash
# Generated by ai-automations — shared constants for the Basecamp project agent.
# Override any value via the environment.

export BC_ACCOUNT_ID="${BC_ACCOUNT_ID:-{{BC_ACCOUNT_ID}}}"
export BC_PROJECT_ID="${BC_PROJECT_ID:-{{BC_PROJECT_ID}}}"
export BC_CARD_TABLE_ID="${BC_CARD_TABLE_ID:-{{BC_CARD_TABLE_ID}}}"
export BC_COL_ANALYZE="${BC_COL_ANALYZE:-{{BC_COL_ANALYZE}}}"
export BC_COL_IMPLEMENT="${BC_COL_IMPLEMENT:-{{BC_COL_IMPLEMENT}}}"
export BC_TODOLIST_ID="${BC_TODOLIST_ID:-{{BC_TODOLIST_ID}}}"
# Enabled event handlers (comma-separated): cards,todos,messages,docs,checkins,schedule
export ENABLED_HANDLERS="${ENABLED_HANDLERS:-{{HANDLERS}}}"
export AGENT_MARKER="${AGENT_MARKER:-{{AGENT_MARKER}}}"
export CLAUDE_MODEL="${CLAUDE_MODEL:-{{CLAUDE_MODEL}}}"
export STATE_BRANCH="${STATE_BRANCH:-basecamp-agent-state}"
export STATE_DIR="${STATE_DIR:-.agent-state}"
export OUT_DIR="${OUT_DIR:-.agent-out}"
