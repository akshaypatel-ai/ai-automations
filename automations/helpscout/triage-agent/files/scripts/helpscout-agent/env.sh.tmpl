#!/usr/bin/env bash
# Generated by ai-automations — shared constants for the Help Scout triage
# agent. Override any value via the environment. HELPSCOUT_APP_ID /
# HELPSCOUT_APP_SECRET come from CI secrets.

# Mailbox to reconcile (the number in the mailbox URL); empty = all mailboxes.
export HELPSCOUT_MAILBOX_ID="${HELPSCOUT_MAILBOX_ID:-{{HELPSCOUT_MAILBOX_ID}}}"
# Label applied to escalated GitHub issues.
export ESCALATION_LABEL="${ESCALATION_LABEL:-{{ESCALATION_LABEL}}}"
# Enabled handlers (comma-separated). Fully implemented: conversations.
export ENABLED_HANDLERS="${ENABLED_HANDLERS:-{{HANDLERS}}}"
export AGENT_MARKER="${AGENT_MARKER:-{{AGENT_MARKER}}}"
export CLAUDE_MODEL="${CLAUDE_MODEL:-{{CLAUDE_MODEL}}}"
export STATE_BRANCH="${STATE_BRANCH:-helpscout-agent-state}"
export STATE_DIR="${STATE_DIR:-.agent-state}"
export OUT_DIR="${OUT_DIR:-.agent-out}"
