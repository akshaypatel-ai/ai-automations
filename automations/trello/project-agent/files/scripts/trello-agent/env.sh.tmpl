#!/usr/bin/env bash
# Generated by ai-automations — shared constants for the Trello project agent.
# Override any value via the environment. TRELLO_KEY/TRELLO_TOKEN come from CI secrets.

export TRELLO_BOARD_ID="${TRELLO_BOARD_ID:-{{TRELLO_BOARD_ID}}}"
export LIST_ANALYZE_ID="${LIST_ANALYZE_ID:-{{LIST_ANALYZE_ID}}}"
export LIST_IMPLEMENT_ID="${LIST_IMPLEMENT_ID:-{{LIST_IMPLEMENT_ID}}}"
# Enabled handlers (comma-separated). Fully implemented: cards.
# lists/members/checklists are routed by the relay but skipped by the resolver
# until their playbooks land — enabling them is harmless, just inert.
export ENABLED_HANDLERS="${ENABLED_HANDLERS:-{{HANDLERS}}}"
export AGENT_MARKER="${AGENT_MARKER:-{{AGENT_MARKER}}}"
export CLAUDE_MODEL="${CLAUDE_MODEL:-{{CLAUDE_MODEL}}}"
export STATE_BRANCH="${STATE_BRANCH:-trello-agent-state}"
export STATE_DIR="${STATE_DIR:-.agent-state}"
export OUT_DIR="${OUT_DIR:-.agent-out}"
