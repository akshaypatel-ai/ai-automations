#!/usr/bin/env bash
# Generated by ai-automations — shared constants for the Teams notify agent.
# Override any value via the environment. TEAMS_WEBHOOK_URL comes from CI secrets.

# Enabled handlers (comma-separated): ship,incident,ask
export ENABLED_HANDLERS="${ENABLED_HANDLERS:-{{HANDLERS}}}"
export AGENT_NAME="${AGENT_NAME:-{{AGENT_NAME}}}"
export CLAUDE_MODEL="${CLAUDE_MODEL:-{{CLAUDE_MODEL}}}"
export OUT_DIR="${OUT_DIR:-.agent-out}"
