#!/usr/bin/env bash
# Generated by ai-automations — shared constants for the Linear project agent.
# Override any value via the environment. LINEAR_API_KEY comes from CI secrets.

export LINEAR_TEAM_KEY="${LINEAR_TEAM_KEY:-{{LINEAR_TEAM_KEY}}}"
# Workflow state NAMES (not IDs) — the analyze state and the implement state.
export STATE_ANALYZE="${STATE_ANALYZE:-{{STATE_ANALYZE}}}"
export STATE_IMPLEMENT="${STATE_IMPLEMENT:-{{STATE_IMPLEMENT}}}"
# Enabled handlers (comma-separated). Fully implemented: issues.
# projects/cycles/docs are routed by the relay but skipped by the resolver
# until their playbooks land — enabling them is harmless, just inert.
export ENABLED_HANDLERS="${ENABLED_HANDLERS:-{{HANDLERS}}}"
export AGENT_MARKER="${AGENT_MARKER:-{{AGENT_MARKER}}}"
export AI_BRAIN="${AI_BRAIN:-{{BRAIN_NAME}}}"
export AI_MODEL="${AI_MODEL:-{{AI_MODEL}}}"
export STATE_BRANCH="${STATE_BRANCH:-linear-agent-state}"
export STATE_DIR="${STATE_DIR:-.agent-state}"
export OUT_DIR="${OUT_DIR:-.agent-out}"
