#!/usr/bin/env bash
# Generated by ai-automations — shared constants for the Asana project agent.
# Override any value via the environment. ASANA_TOKEN comes from CI secrets.

export ASANA_PROJECT_GID="${ASANA_PROJECT_GID:-{{ASANA_PROJECT_GID}}}"
# Section NAMES within the project (matching is case-insensitive).
export SECTION_ANALYZE="${SECTION_ANALYZE:-{{SECTION_ANALYZE}}}"
export SECTION_IMPLEMENT="${SECTION_IMPLEMENT:-{{SECTION_IMPLEMENT}}}"
# Enabled handlers (comma-separated). Fully implemented: tasks.
# sections/projects are routed by the relay but skipped by the resolver
# until their playbooks land — enabling them is harmless, just inert.
export ENABLED_HANDLERS="${ENABLED_HANDLERS:-{{HANDLERS}}}"
export AGENT_MARKER="${AGENT_MARKER:-{{AGENT_MARKER}}}"
export AI_BRAIN="${AI_BRAIN:-{{BRAIN_NAME}}}"
export AI_MODEL="${AI_MODEL:-{{AI_MODEL}}}"
export STATE_BRANCH="${STATE_BRANCH:-asana-agent-state}"
export STATE_DIR="${STATE_DIR:-.agent-state}"
export OUT_DIR="${OUT_DIR:-.agent-out}"
