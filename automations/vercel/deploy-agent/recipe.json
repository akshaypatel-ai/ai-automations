{
  "name": "Vercel Deploy Agent",
  "description": "Turns failed Vercel deployments into repo-grounded triage issues: fetches the deployment + its build logs, reads the repo at the failing build step, and files ONE GitHub issue (what failed, log excerpt, likely cause, where to look). New failures of the same commit become comments — never duplicates. Signed-webhook relay; the deployments handler is fully implemented.",
  "tool": "vercel",
  "status": "beta",
  "runtime": "github-actions",
  "brains": ["claude-code"],
  "handlers": ["deployments"],
  "requires": ["git", "jq", "gh", "curl", "wrangler"]
}
