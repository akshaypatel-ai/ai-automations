#!/usr/bin/env bash
# Generated by ai-automations — shared constants for the Slack triage agent.
# Override any value via the environment. SLACK_BOT_TOKEN comes from CI secrets.

# Channel IDs the triage handler watches (comma-separated C… ids).
export WATCHED_CHANNELS="${WATCHED_CHANNELS:-{{WATCHED_CHANNELS}}}"
# Emoji names (no colons) that explicitly summon the agent onto a message.
export TRIGGER_EMOJI="${TRIGGER_EMOJI:-{{TRIGGER_EMOJI}}}"
# Enabled handlers (comma-separated): mentions,channel,reactions,dm
export ENABLED_HANDLERS="${ENABLED_HANDLERS:-{{HANDLERS}}}"
export CLAUDE_MODEL="${CLAUDE_MODEL:-{{CLAUDE_MODEL}}}"
export STATE_BRANCH="${STATE_BRANCH:-slack-agent-state}"
export STATE_DIR="${STATE_DIR:-.agent-state}"
export OUT_DIR="${OUT_DIR:-.agent-out}"
