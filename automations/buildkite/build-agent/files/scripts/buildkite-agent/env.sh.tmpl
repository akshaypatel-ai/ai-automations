#!/usr/bin/env bash
# Generated by ai-automations — shared constants for the Buildkite build agent.
# Override any value via the environment. BUILDKITE_TOKEN comes from CI secrets.

# Organization slug — the <org> in buildkite.com/<org>.
export BUILDKITE_ORG="${BUILDKITE_ORG:-{{BUILDKITE_ORG}}}"
# Pipeline slug — the <pipeline> in buildkite.com/<org>/<pipeline>.
export BUILDKITE_PIPELINE="${BUILDKITE_PIPELINE:-{{BUILDKITE_PIPELINE}}}"
# Label applied to every GitHub issue the agent files.
export ISSUE_LABEL="${ISSUE_LABEL:-{{ISSUE_LABEL}}}"
# Enabled handlers (comma-separated). Fully implemented: builds.
export ENABLED_HANDLERS="${ENABLED_HANDLERS:-{{HANDLERS}}}"
export AGENT_MARKER="${AGENT_MARKER:-{{AGENT_MARKER}}}"
export CLAUDE_MODEL="${CLAUDE_MODEL:-{{CLAUDE_MODEL}}}"
export STATE_BRANCH="${STATE_BRANCH:-buildkite-agent-state}"
export STATE_DIR="${STATE_DIR:-.agent-state}"
export OUT_DIR="${OUT_DIR:-.agent-out}"
