#!/usr/bin/env bash
# Generated by ai-automations — shared constants for the Confluence docs agent.
# Override any value via the environment. CONFLUENCE_EMAIL/CONFLUENCE_API_TOKEN
# come from CI secrets.

export CONFLUENCE_SITE="${CONFLUENCE_SITE:-{{CONFLUENCE_SITE}}}"
# Label that asks for a review when added to a page.
export REVIEW_LABEL="${REVIEW_LABEL:-{{REVIEW_LABEL}}}"
# Enabled handlers (comma-separated). Fully implemented: pages.
export ENABLED_HANDLERS="${ENABLED_HANDLERS:-{{HANDLERS}}}"
export AGENT_MARKER="${AGENT_MARKER:-{{AGENT_MARKER}}}"
export AI_BRAIN="${AI_BRAIN:-{{BRAIN_NAME}}}"
export AI_MODEL="${AI_MODEL:-{{AI_MODEL}}}"
export STATE_BRANCH="${STATE_BRANCH:-confluence-agent-state}"
export STATE_DIR="${STATE_DIR:-.agent-state}"
export OUT_DIR="${OUT_DIR:-.agent-out}"
