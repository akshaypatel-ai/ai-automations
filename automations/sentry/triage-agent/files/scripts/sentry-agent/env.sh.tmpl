#!/usr/bin/env bash
# Generated by ai-automations — shared constants for the Sentry triage agent.
# Override any value via the environment. SENTRY_TOKEN comes from CI secrets.

# API base URL — point this at your instance for self-hosted Sentry.
export SENTRY_API_BASE="${SENTRY_API_BASE:-{{SENTRY_API_BASE}}}"
export SENTRY_ORG="${SENTRY_ORG:-{{SENTRY_ORG}}}"
export SENTRY_PROJECT="${SENTRY_PROJECT:-{{SENTRY_PROJECT}}}"
# Label applied to every GitHub issue the agent files.
export ISSUE_LABEL="${ISSUE_LABEL:-{{ISSUE_LABEL}}}"
# Enabled handlers (comma-separated). Fully implemented: issues.
export ENABLED_HANDLERS="${ENABLED_HANDLERS:-{{HANDLERS}}}"
export AGENT_MARKER="${AGENT_MARKER:-{{AGENT_MARKER}}}"
export CLAUDE_MODEL="${CLAUDE_MODEL:-{{CLAUDE_MODEL}}}"
export STATE_BRANCH="${STATE_BRANCH:-sentry-agent-state}"
export STATE_DIR="${STATE_DIR:-.agent-state}"
export OUT_DIR="${OUT_DIR:-.agent-out}"
