#!/usr/bin/env bash
# Generated by ai-automations — shared constants for the Zendesk triage agent.
# Override any value via the environment. ZENDESK_EMAIL/ZENDESK_API_TOKEN come
# from CI secrets.

export ZENDESK_SUBDOMAIN="${ZENDESK_SUBDOMAIN:-{{ZENDESK_SUBDOMAIN}}}"
# Label applied to escalated GitHub issues.
export ESCALATION_LABEL="${ESCALATION_LABEL:-{{ESCALATION_LABEL}}}"
# Enabled handlers (comma-separated). Fully implemented: tickets.
export ENABLED_HANDLERS="${ENABLED_HANDLERS:-{{HANDLERS}}}"
export AGENT_MARKER="${AGENT_MARKER:-{{AGENT_MARKER}}}"
export CLAUDE_MODEL="${CLAUDE_MODEL:-{{CLAUDE_MODEL}}}"
export STATE_BRANCH="${STATE_BRANCH:-zendesk-agent-state}"
export STATE_DIR="${STATE_DIR:-.agent-state}"
export OUT_DIR="${OUT_DIR:-.agent-out}"
