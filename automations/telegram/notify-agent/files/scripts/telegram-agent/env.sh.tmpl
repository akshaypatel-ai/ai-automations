#!/usr/bin/env bash
# Generated by ai-automations — shared constants for the Telegram notify agent.
# Override any value via the environment. TELEGRAM_BOT_TOKEN comes from CI secrets.

# Default push target: the chat id (group/channel/user; groups are negative numbers).
export TELEGRAM_CHAT_ID="${TELEGRAM_CHAT_ID:-{{TELEGRAM_CHAT_ID}}}"
# Enabled handlers (comma-separated): ship,incident,ask
export ENABLED_HANDLERS="${ENABLED_HANDLERS:-{{HANDLERS}}}"
export AGENT_NAME="${AGENT_NAME:-{{AGENT_NAME}}}"
export AI_BRAIN="${AI_BRAIN:-{{BRAIN_NAME}}}"
export AI_MODEL="${AI_MODEL:-{{AI_MODEL}}}"
export OUT_DIR="${OUT_DIR:-.agent-out}"
