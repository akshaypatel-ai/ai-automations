#!/usr/bin/env bash
# Generated by ai-automations — shared constants for the GitHub Issues agent.
# Override any value via the environment. Auth comes from GH_TOKEN in CI.

export REPO="${REPO:-{{GITHUB_REPO}}}"
# Label NAMES that drive the flow (exact match, case-sensitive like GitHub).
export LABEL_ANALYZE="${LABEL_ANALYZE:-{{LABEL_ANALYZE}}}"
export LABEL_IMPLEMENT="${LABEL_IMPLEMENT:-{{LABEL_IMPLEMENT}}}"
# Enabled handlers (comma-separated). Fully implemented: issues.
export ENABLED_HANDLERS="${ENABLED_HANDLERS:-{{HANDLERS}}}"
export AGENT_MARKER="${AGENT_MARKER:-{{AGENT_MARKER}}}"
export AI_BRAIN="${AI_BRAIN:-{{BRAIN_NAME}}}"
export AI_MODEL="${AI_MODEL:-{{AI_MODEL}}}"
export STATE_BRANCH="${STATE_BRANCH:-issues-agent-state}"
export STATE_DIR="${STATE_DIR:-.agent-state}"
export OUT_DIR="${OUT_DIR:-.agent-out}"
