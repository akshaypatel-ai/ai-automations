#!/usr/bin/env bash
# Generated by ai-automations — shared constants for the LINE notify agent.
# Override any value via the environment. LINE_CHANNEL_ACCESS_TOKEN comes from CI secrets.

# Default push target: a group (C…), room (R…), or user (U…) id.
export LINE_TARGET_ID="${LINE_TARGET_ID:-{{LINE_TARGET_ID}}}"
# Enabled handlers (comma-separated): ship,incident,ask
export ENABLED_HANDLERS="${ENABLED_HANDLERS:-{{HANDLERS}}}"
export AI_BRAIN="${AI_BRAIN:-{{BRAIN_NAME}}}"
export AI_MODEL="${AI_MODEL:-{{AI_MODEL}}}"
export OUT_DIR="${OUT_DIR:-.agent-out}"
