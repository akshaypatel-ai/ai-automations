{
  "name": "LINE Notify Agent",
  "description": "AI-written ship/deploy/incident notifications pushed to LINE via GitHub-native triggers (no relay), plus optional inbound group Q&A through a signed-webhook relay.",
  "tool": "line",
  "status": "beta",
  "runtime": "github-actions",
  "brains": ["claude-code", "codex", "gemini-cli"],
  "handlers": ["ship", "incident", "ask"],
  "requires": ["git", "jq", "gh", "curl"]
}
