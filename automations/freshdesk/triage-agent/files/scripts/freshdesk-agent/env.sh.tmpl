#!/usr/bin/env bash
# Generated by ai-automations — shared constants for the Freshdesk triage agent.
# Override any value via the environment. FRESHDESK_API_KEY comes from CI
# secrets.

export FRESHDESK_DOMAIN="${FRESHDESK_DOMAIN:-{{FRESHDESK_DOMAIN}}}"
# Label applied to escalated GitHub issues.
export ESCALATION_LABEL="${ESCALATION_LABEL:-{{ESCALATION_LABEL}}}"
# Enabled handlers (comma-separated). Fully implemented: tickets.
export ENABLED_HANDLERS="${ENABLED_HANDLERS:-{{HANDLERS}}}"
export AGENT_MARKER="${AGENT_MARKER:-{{AGENT_MARKER}}}"
export AI_BRAIN="${AI_BRAIN:-{{BRAIN_NAME}}}"
export AI_MODEL="${AI_MODEL:-{{AI_MODEL}}}"
export STATE_BRANCH="${STATE_BRANCH:-freshdesk-agent-state}"
export STATE_DIR="${STATE_DIR:-.agent-state}"
export OUT_DIR="${OUT_DIR:-.agent-out}"
