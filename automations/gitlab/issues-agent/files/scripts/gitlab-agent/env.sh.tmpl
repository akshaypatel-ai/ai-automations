#!/usr/bin/env bash
# Generated by ai-automations — shared constants for the GitLab Issues agent.
# Override any value via the environment. GITLAB_TOKEN comes from CI secrets.

# API base — https://gitlab.com or your self-hosted instance.
export GITLAB_API_BASE="${GITLAB_API_BASE:-{{GITLAB_API_BASE}}}"
export GITLAB_PROJECT_ID="${GITLAB_PROJECT_ID:-{{GITLAB_PROJECT_ID}}}"
# Label NAMES that drive the flow (exact match). GitLab boards are label-driven,
# so these are the board's native columns.
export LABEL_ANALYZE="${LABEL_ANALYZE:-{{LABEL_ANALYZE}}}"
export LABEL_IMPLEMENT="${LABEL_IMPLEMENT:-{{LABEL_IMPLEMENT}}}"
# Enabled handlers (comma-separated). Fully implemented: issues.
export ENABLED_HANDLERS="${ENABLED_HANDLERS:-{{HANDLERS}}}"
export AGENT_MARKER="${AGENT_MARKER:-{{AGENT_MARKER}}}"
export CLAUDE_MODEL="${CLAUDE_MODEL:-{{CLAUDE_MODEL}}}"
export STATE_BRANCH="${STATE_BRANCH:-gitlab-agent-state}"
export STATE_DIR="${STATE_DIR:-.agent-state}"
export OUT_DIR="${OUT_DIR:-.agent-out}"
