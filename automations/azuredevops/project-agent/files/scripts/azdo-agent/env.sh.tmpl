#!/usr/bin/env bash
# Generated by ai-automations — shared constants for the Azure DevOps project agent.
# Override any value via the environment. AZDO_PAT comes from CI secrets.

export AZDO_ORG="${AZDO_ORG:-{{AZDO_ORG}}}"
export AZDO_PROJECT="${AZDO_PROJECT:-{{AZDO_PROJECT}}}"
# State DISPLAY NAMES — the analyze state and the implement state
# (matching is case-insensitive).
export STATE_ANALYZE="${STATE_ANALYZE:-{{STATE_ANALYZE}}}"
export STATE_IMPLEMENT="${STATE_IMPLEMENT:-{{STATE_IMPLEMENT}}}"
# Enabled handlers (comma-separated). Fully implemented: workitems.
export ENABLED_HANDLERS="${ENABLED_HANDLERS:-{{HANDLERS}}}"
export AGENT_MARKER="${AGENT_MARKER:-{{AGENT_MARKER}}}"
export CLAUDE_MODEL="${CLAUDE_MODEL:-{{CLAUDE_MODEL}}}"
export STATE_BRANCH="${STATE_BRANCH:-azdo-agent-state}"
export STATE_DIR="${STATE_DIR:-.agent-state}"
export OUT_DIR="${OUT_DIR:-.agent-out}"
